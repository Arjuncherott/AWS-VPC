The VPC has public subnet and private subnets in two availablity zones

Each public subnet contains a NAT Gateway and a load balancer

The servers run in the private subnets, are launched and terminating by using an Auto-Scaling group, and recieve traffic from the load balancer.

The server can connect to the internet using NAT gateway.
